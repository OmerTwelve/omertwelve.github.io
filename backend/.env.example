# Copy to .env and fill in. Never commit .env — it is gitignored.
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides (defaults shown in config.py)
# CLAUDE_MODEL=claude-opus-5
# ALLOWED_ORIGINS=https://omertwelve.github.io
# RATE_PER_MINUTE=6
# RATE_PER_DAY=60
